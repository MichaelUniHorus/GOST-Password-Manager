# ❓ Часто задаваемые вопросы (FAQ)

## Общие вопросы

### Что такое менеджер паролей?

Менеджер паролей - это приложение для безопасного хранения всех ваших паролей в одном зашифрованном месте. Вам нужно помнить только один мастер-пароль для доступа ко всем остальным.

### Чем это приложение отличается от других менеджеров паролей?

- ✅ **Локальное хранение** - все данные только на вашем компьютере
- ✅ **ГОСТ-криптография** - соответствие российским стандартам 2025 года
- ✅ **Open Source** - открытый исходный код
- ✅ **Бесплатно** - без подписок и ограничений
- ✅ **Zero-knowledge** - никто кроме вас не имеет доступа к данным

### Безопасно ли это?

Да, при правильном использовании:
- Используется криптография по ГОСТ Р 34.12-2015 и ГОСТ Р 34.11-2012
- Мастер-пароль хэшируется с Argon2id (защита от brute-force)
- Все данные шифруются локально перед сохранением
- Никакие данные не отправляются в интернет

**Но помните**: безопасность зависит от:
- Надежности мастер-пароля
- Защиты вашего компьютера
- Регулярных резервных копий

## Установка и настройка

### Какие требования к системе?

- **ОС**: Windows 10/11, Linux, macOS
- **Python**: 3.9 или новее
- **RAM**: Минимум 2 GB
- **Диск**: 100 MB свободного места

### Как установить Python?

**Windows**:
1. Скачайте с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. ✅ Отметьте "Add Python to PATH"
4. Нажмите "Install Now"

**Linux**:
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

**macOS**:
```bash
brew install python3
```

### Ошибка "pip не найден"

**Windows**:
```bash
python -m pip install --upgrade pip
```

**Linux/macOS**:
```bash
python3 -m pip install --upgrade pip
```

### Ошибка при установке зависимостей

Попробуйте:
```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

Если не помогает:
```bash
pip install --no-cache-dir -r requirements.txt
```

## Использование

### Как создать мастер-пароль?

1. Запустите приложение
2. При первом запуске появится форма создания мастер-пароля
3. Придумайте надежный пароль (минимум 15 символов)
4. Подтвердите пароль
5. Нажмите "Создать мастер-пароль"

**Важно**: Запишите мастер-пароль в безопасном месте!

### Что делать, если забыл мастер-пароль?

К сожалению, **восстановление невозможно**. Это особенность zero-knowledge архитектуры - никто кроме вас не знает пароль.

**Рекомендации**:
- Запишите мастер-пароль на бумаге и храните в сейфе
- Используйте мнемонические техники для запоминания
- Регулярно входите в приложение, чтобы не забыть пароль

### Как добавить новый пароль?

1. Войдите с мастер-паролем
2. Нажмите "+ Добавить запись"
3. Заполните поля:
   - Название сайта (обязательно)
   - URL (опционально)
   - Логин (обязательно)
   - Пароль (обязательно)
4. Нажмите "Сохранить"

### Как сгенерировать надежный пароль?

1. При создании/редактировании записи нажмите "🎲 Генерировать"
2. Настройте параметры:
   - Длина (рекомендуется 20+ символов)
   - Типы символов
3. Нажмите "Генерировать" для создания нового пароля
4. Нажмите "Использовать" для применения

### Как настроить TOTP (2FA)?

1. Получите TOTP-секрет от сервиса (обычно показывается при настройке 2FA)
2. При создании записи вставьте секрет в поле "TOTP секрет"
3. Сохраните запись
4. Для получения кода нажмите "Показать TOTP" в карточке записи

### Как скопировать пароль?

Нажмите кнопку 📋 рядом с полем пароля. Появится уведомление "Скопировано в буфер обмена".

### Как найти нужную запись?

Используйте поле поиска вверху страницы. Поиск работает по:
- Названию сайта
- Логину
- URL

## Безопасность

### Как создать резервную копию?

**Способ 1 - Вручную**:
```bash
copy password_manager.db backup_2025-11-30.db
```

**Способ 2 - Скрипт**:
```bash
python backup.py create
```

### Как восстановить из резервной копии?

```bash
python backup.py restore --file backups/password_manager_backup_2025-11-30_12-00-00.db
```

### Где хранятся данные?

В файле `password_manager.db` в директории приложения. Все данные в этом файле зашифрованы.

### Можно ли использовать на нескольких компьютерах?

Да, но синхронизация не автоматическая:
1. Скопируйте файл `password_manager.db` на другой компьютер
2. Установите приложение на другом компьютере
3. Замените файл БД

**Важно**: Используйте зашифрованные каналы передачи (USB, зашифрованное облако).

### Безопасно ли хранить БД в облаке?

Да, если:
- ✅ Используете зашифрованное облако (Dropbox, Google Drive с шифрованием)
- ✅ У вас надежный мастер-пароль
- ✅ Включена двухфакторная аутентификация на облаке

Даже если кто-то получит доступ к файлу БД, без мастер-пароля расшифровать данные невозможно.

### Что делать при подозрении на взлом?

1. **Немедленно смените мастер-пароль** (пересоздайте БД)
2. **Смените все пароли** в сервисах
3. **Проверьте журнал аудита** (кнопка "📊 Аудит")
4. **Проверьте систему** антивирусом
5. **Включите 2FA** везде, где возможно

## Технические вопросы

### Браузер показывает предупреждение о безопасности

Это нормально! Приложение использует self-signed SSL сертификат для HTTPS.

**Как обойти**:
- **Chrome**: Нажмите "Дополнительно" → "Перейти на сайт"
- **Firefox**: Нажмите "Дополнительно" → "Принять риск"
- **Edge**: Нажмите "Подробности" → "Перейти на веб-страницу"

### Порт 5000 уже занят

Измените порт в файле `app.py`:
```python
app.run(host='127.0.0.1', port=5001, debug=True, ssl_context='adhoc')
```

### Приложение не запускается

**Проверьте**:
1. Python установлен: `python --version`
2. Зависимости установлены: `pip list`
3. Порт 5000 свободен: `netstat -an | findstr 5000`
4. Файл `.env` существует

**Посмотрите логи** в консоли для деталей ошибки.

### Ошибка "ModuleNotFoundError"

Установите недостающий модуль:
```bash
pip install -r requirements.txt
```

Или конкретный модуль:
```bash
pip install flask sqlalchemy pygost
```

### Как обновить приложение?

1. Сохраните резервную копию БД
2. Скачайте новую версию
3. Скопируйте файл `password_manager.db` в новую директорию
4. Установите зависимости: `pip install -r requirements.txt`
5. Запустите приложение

### Можно ли использовать с настоящим SSL сертификатом?

Да, замените в `app.py`:
```python
# Вместо
app.run(ssl_context='adhoc')

# Используйте
app.run(ssl_context=('path/to/cert.pem', 'path/to/key.pem'))
```

## Производительность

### Приложение работает медленно

**Причины**:
- Много записей (>1000)
- Слабый компьютер
- Антивирус блокирует операции

**Решения**:
- Добавьте приложение в исключения антивируса
- Используйте SSD вместо HDD
- Закройте другие программы

### Сколько паролей можно хранить?

Теоретически - неограниченно. Практически:
- **До 1000** - отличная производительность
- **1000-5000** - хорошая производительность
- **5000+** - может потребоваться оптимизация

## Интеграция

### Можно ли импортировать пароли из Chrome/Firefox?

В текущей версии - нет. Планируется в будущих обновлениях.

**Временное решение**:
1. Экспортируйте пароли из браузера в CSV
2. Вручную добавьте в менеджер паролей

### Можно ли экспортировать в KeePass?

Планируется в будущих версиях. Сейчас можно только создать резервную копию БД.

### Есть ли мобильное приложение?

Нет, только веб-интерфейс для локального использования.

### Есть ли расширение для браузера?

Нет, но можно использовать копирование паролей через веб-интерфейс.

## Разработка

### Можно ли модифицировать код?

Да! Проект с открытым исходным кодом. Вы можете:
- Изменять код под свои нужды
- Добавлять новые функции
- Исправлять ошибки
- Делиться улучшениями

### Как добавить новую функцию?

1. Изучите существующий код
2. Добавьте функцию в соответствующий модуль
3. Обновите API endpoints в `app.py`
4. Добавьте UI в `static/`
5. Протестируйте изменения

### Где сообщить об ошибке?

Создайте issue в репозитории проекта с описанием:
- Версия Python
- ОС
- Шаги для воспроизведения
- Текст ошибки

## Дополнительно

### Можно ли использовать в коммерческих целях?

Да, проект под лицензией MIT - свободное использование.

### Есть ли техподдержка?

Проект поддерживается сообществом. Задавайте вопросы в issues.

### Планируются ли новые функции?

Да, в планах:
- Импорт/экспорт паролей
- Смена мастер-пароля
- Биометрическая аутентификация
- Мобильное приложение
- Синхронизация между устройствами

### Как поддержать проект?

- ⭐ Поставьте звезду на GitHub
- 🐛 Сообщайте об ошибках
- 💡 Предлагайте улучшения
- 🔧 Вносите изменения в код
- 📢 Рассказывайте друзьям

---

**Не нашли ответ на свой вопрос?** Создайте issue в репозитории проекта!
