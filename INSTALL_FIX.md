# 🔧 Исправление проблемы с pygost

## Что изменилось

Заменили `pygost` на `gostcrypto` - более современную и доступную библиотеку ГОСТ.

## ✅ Быстрая установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Проверьте установку

```bash
python -c "from gostcrypto import gosthash, gostcipher; print('✅ gostcrypto установлен успешно!')"
```

### 3. Запустите приложение

```bash
python app.py
```

## 📦 Что установится

- **gostcrypto** (вместо pygost) - ГОСТ криптография
- **Flask** - веб-фреймворк
- **SQLAlchemy** - ORM для базы данных
- **pyotp** - TOTP (2FA)
- **argon2-cffi** - хэширование паролей
- **cryptography** - дополнительная криптография
- **pyOpenSSL** - HTTPS поддержка

## 🔍 Технические детали

### Изменения в коде

**Было (pygost)**:
```python
from pygost.gost3412 import GOST3412Kuznechik
from pygost import gost34112012256, gost34112012512

cipher = GOST3412Kuznechik(key)
hash_obj = gost34112012512.new(data)
```

**Стало (gostcrypto)**:
```python
from gostcrypto import gosthash, gostcipher

cipher = gostcipher.new('kuznechik', key, gostcipher.MODE_CTR, init_vect=nonce)
hash_obj = gosthash.new('streebog512', data=data)
```

### Преимущества gostcrypto

- ✅ Доступен на PyPI без проблем
- ✅ Активно поддерживается
- ✅ Более простой API
- ✅ Лучшая документация
- ✅ Совместим с Windows/Linux/macOS

## ❓ Возможные проблемы

### Ошибка: "No module named 'gostcrypto'"

```bash
pip install --upgrade pip
pip install gostcrypto
```

### Ошибка при установке других пакетов

Установите по одному:
```bash
pip install Flask
pip install SQLAlchemy
pip install gostcrypto
pip install pyotp
pip install argon2-cffi
```

### Проблемы с Pillow на Windows

```bash
pip install --upgrade Pillow
```

## 🎯 Проверка работоспособности

После установки запустите:

```bash
python -c "
from crypto_gost import get_crypto
crypto = get_crypto()
print('✅ Криптомодуль работает!')
print('✅ Стрибог-512:', crypto.streebog_512(b'test').hex()[:16] + '...')
print('✅ Генерация пароля:', crypto.generate_secure_password(20))
"
```

Должно вывести:
```
✅ Криптомодуль работает!
✅ Стрибог-512: 3b5d3c7d207e37...
✅ Генерация пароля: <случайный пароль>
```

## 📚 Дополнительная информация

- **gostcrypto документация**: https://github.com/drobotun/gostcrypto
- **ГОСТ Р 34.12-2015**: Кузнечик (блочный шифр)
- **ГОСТ Р 34.11-2012**: Стрибог (хэш-функция)

---

**Готово!** Теперь можно запускать приложение: `python app.py`
