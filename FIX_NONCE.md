# 🔧 Исправление ошибки с nonce

## Проблема

Библиотека `gostcrypto` требует, чтобы `init_vect` (nonce) для режима CTR был **ровно половиной размера блока**:
- Размер блока Кузнечика: 128 бит (16 байт)
- Требуемый размер nonce: **64 бит (8 байт)**

## Что было исправлено

### 1. `crypto_gost.py`

**Было**:
```python
NONCE_LENGTH = 16  # 128 бит для CTR
```

**Стало**:
```python
NONCE_LENGTH = 8  # 64 бит для CTR (половина размера блока Кузнечика)
```

### 2. `test_crypto.py`

**Было**:
```python
assert len(nonce1) == 16
assert len(nonce2) == 16
```

**Стало**:
```python
assert len(nonce1) == 8  # 64 бит для CTR режима
assert len(nonce2) == 8
```

## Проверка исправления

Запустите тесты:

```bash
pytest test_crypto.py -v
```

**Ожидаемый результат**: Все 16 тестов должны пройти ✅

## Почему это важно

Режим CTR в `gostcrypto` использует счетчик, который занимает половину блока:
- Первые 8 байт: nonce (уникальный для каждого шифрования)
- Вторые 8 байт: счетчик (увеличивается для каждого блока)

Это соответствует стандарту ГОСТ Р 34.13-2015.

## Влияние на существующие данные

⚠️ **Важно**: Если у вас уже есть зашифрованные данные с nonce=16 байт, они **не смогут быть расшифрованы** после этого изменения.

**Решение**: Создайте резервную копию БД перед обновлением.

---

**Статус**: ✅ Исправлено
