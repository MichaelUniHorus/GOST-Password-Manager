# 📁 Структура проекта

```
password-manager-gost/
│
├── 📄 app.py                    # Главное Flask приложение (backend)
├── 📄 models.py                 # Модели базы данных (SQLAlchemy)
├── 📄 crypto_gost.py            # Криптографический модуль (ГОСТ)
├── 📄 backup.py                 # Скрипт резервного копирования
│
├── 📄 requirements.txt          # Python зависимости
├── 📄 .env.example              # Пример конфигурации
├── 📄 .gitignore               # Исключения для git
│
├── 🚀 start.bat                 # Скрипт запуска (Windows)
├── 🚀 start.sh                  # Скрипт запуска (Linux/macOS)
│
├── 📖 README.md                 # Основная документация
├── 📖 QUICKSTART.md             # Быстрый старт
├── 📖 FAQ.md                    # Часто задаваемые вопросы
├── 📖 SECURITY.md               # Руководство по безопасности
├── 📖 CHANGELOG.md              # История изменений
├── 📖 PROJECT_STRUCTURE.md      # Этот файл
├── 📄 LICENSE                   # Лицензия MIT
│
├── 📁 static/                   # Frontend файлы
│   ├── 📄 index.html           # HTML интерфейс
│   ├── 📄 style.css            # Стили
│   └── 📄 app.js               # JavaScript логика
│
├── 📁 backups/                  # Резервные копии (создается автоматически)
│   └── 📄 *.db                 # Файлы резервных копий
│
└── 📄 password_manager.db       # База данных (создается при первом запуске)
```

## 📋 Описание файлов

### Backend (Python)

#### `app.py` (16 KB)
**Назначение**: Главное приложение Flask с API endpoints

**Основные компоненты**:
- Инициализация Flask приложения
- API endpoints для аутентификации
- API endpoints для управления записями
- Генератор паролей
- TOTP поддержка
- Журнал аудита
- Защита от brute-force
- Управление сессиями

**Endpoints**:
```
GET  /                          # Главная страница
GET  /api/init                  # Проверка инициализации
POST /api/init                  # Создание мастер-пароля
POST /api/login                 # Вход
POST /api/logout                # Выход
GET  /api/entries               # Список записей
POST /api/entries               # Создание записи
PUT  /api/entries/<id>          # Обновление записи
DELETE /api/entries/<id>        # Удаление записи
POST /api/generate-password     # Генерация пароля
GET  /api/totp/<id>             # Получение TOTP кода
GET  /api/audit-logs            # Журнал аудита
```

#### `models.py` (6 KB)
**Назначение**: Модели базы данных SQLAlchemy

**Таблицы**:
- `master_password` - Хэш мастер-пароля
- `password_entries` - Записи паролей (зашифрованные)
- `audit_logs` - Журнал аудита
- `login_attempts` - Попытки входа (для rate limiting)
- `session_tokens` - Сессионные токены

**Класс Database**:
- Управление подключением к БД
- Инициализация таблиц
- Создание сессий
- Резервное копирование

#### `crypto_gost.py` (10 KB)
**Назначение**: Криптографические операции по ГОСТ

**Класс GOSTCrypto**:
- `generate_salt()` - Генерация соли
- `generate_nonce()` - Генерация nonce для CTR
- `streebog_512()` - Хэширование Стрибог-512
- `streebog_256()` - Хэширование Стрибог-256
- `derive_key_pbkdf2_gost()` - Деривация ключа из пароля
- `hash_master_password()` - Хэширование мастер-пароля (Argon2id)
- `verify_master_password()` - Проверка мастер-пароля
- `encrypt_data()` - Шифрование данных (Кузнечик CTR)
- `decrypt_data()` - Расшифрование данных
- `generate_secure_password()` - Генерация паролей
- `validate_password_strength()` - Проверка стойкости

**Алгоритмы**:
- ГОСТ Р 34.12-2015 (Кузнечик) - шифрование
- ГОСТ Р 34.11-2012 (Стрибог) - хэширование
- Argon2id - хэширование мастер-пароля
- PBKDF2 - деривация ключей

#### `backup.py` (8 KB)
**Назначение**: Управление резервными копиями

**Функции**:
- `create_backup()` - Создание резервной копии
- `list_backups()` - Список всех копий
- `restore_backup()` - Восстановление из копии
- `cleanup_old_backups()` - Удаление старых копий

**Использование**:
```bash
python backup.py create              # Создать копию
python backup.py list                # Список копий
python backup.py restore --file ...  # Восстановить
python backup.py cleanup --keep 10   # Очистить старые
```

### Frontend (HTML/CSS/JavaScript)

#### `static/index.html` (10 KB)
**Назначение**: HTML структура интерфейса

**Экраны**:
- Экран инициализации (создание мастер-пароля)
- Экран входа
- Основной экран (список записей)

**Модальные окна**:
- Добавление/редактирование записи
- Генератор паролей
- Журнал аудита
- TOTP код

#### `static/style.css` (10 KB)
**Назначение**: Стили интерфейса

**Особенности**:
- Современный дизайн с градиентами
- Адаптивная верстка (mobile-first)
- CSS переменные для цветов
- Анимации и переходы
- Темная/светлая тема (через переменные)

**Компоненты**:
- Карточки записей
- Формы ввода
- Кнопки
- Модальные окна
- Уведомления

#### `static/app.js` (19 KB)
**Назначение**: Клиентская логика

**Основные функции**:
- `checkInitialization()` - Проверка инициализации
- `login()` - Вход в систему
- `logout()` - Выход
- `loadEntries()` - Загрузка записей
- `renderEntries()` - Отображение записей
- `createEntry()` - Создание записи
- `updateEntry()` - Обновление записи
- `deleteEntry()` - Удаление записи
- `generatePassword()` - Генерация пароля
- `showTOTP()` - Показ TOTP кода
- `copyToClipboard()` - Копирование в буфер

**Особенности**:
- Автоблокировка при неактивности (5 мин)
- Поиск в реальном времени
- Валидация форм
- Обработка ошибок
- Уведомления

### Конфигурация

#### `requirements.txt`
**Зависимости Python**:
```
Flask==3.0.0              # Веб-фреймворк
Flask-CORS==4.0.0         # CORS поддержка
SQLAlchemy==2.0.23        # ORM
pygost==5.12              # ГОСТ криптография
pyotp==2.9.0              # TOTP
argon2-cffi==23.1.0       # Argon2 хэширование
zxcvbn==4.4.28            # Оценка паролей
cryptography==41.0.7      # Дополнительная криптография
pysqlcipher3==1.2.0       # SQLite с шифрованием
qrcode==7.4.2             # QR коды
Pillow==10.1.0            # Обработка изображений
python-dotenv==1.0.0      # .env файлы
```

#### `.env.example`
**Переменные окружения**:
```env
SECRET_KEY=...            # Секретный ключ Flask
DATABASE_PATH=...         # Путь к БД
SESSION_TIMEOUT=300       # Таймаут сессии (сек)
MAX_LOGIN_ATTEMPTS=5      # Макс попыток входа
LOCKOUT_DURATION=30       # Блокировка (сек)
```

#### `.gitignore`
**Исключения для git**:
- `*.db` - База данных
- `.env` - Конфигурация
- `__pycache__/` - Python кэш
- `venv/` - Виртуальное окружение
- `*.log` - Логи
- `certs/` - Сертификаты

### Документация

#### `README.md` (12 KB)
- Описание проекта
- Особенности
- Требования
- Установка и запуск
- Использование
- Архитектура
- Криптографические детали
- Соответствие ГОСТ

#### `QUICKSTART.md` (5 KB)
- Быстрый старт за 5 минут
- Пошаговая инструкция
- Основные функции
- Резервное копирование
- Советы по безопасности

#### `FAQ.md` (14 KB)
- Часто задаваемые вопросы
- Решение проблем
- Технические детали
- Интеграция
- Разработка

#### `SECURITY.md` (11 KB)
- Руководство по безопасности
- Криптографические гарантии
- Защита от атак
- Рекомендации
- Контрольный список

#### `CHANGELOG.md` (5 KB)
- История изменений
- Версии
- Планы развития

### Скрипты запуска

#### `start.bat` (Windows)
```batch
- Создание venv
- Активация venv
- Установка зависимостей
- Создание .env
- Запуск приложения
```

#### `start.sh` (Linux/macOS)
```bash
- Создание venv
- Активация venv
- Установка зависимостей
- Создание .env
- Запуск приложения
```

## 🔄 Поток данных

### Инициализация
```
Пользователь → Мастер-пароль → Argon2id → Хэш → БД
                              ↓
                         PBKDF2-ГОСТ → Ключ шифрования → Память
```

### Сохранение пароля
```
Пароль → Кузнечик CTR (ключ из мастер-пароля) → Зашифрованный текст → БД
```

### Получение пароля
```
БД → Зашифрованный текст → Кузнечик CTR (ключ из сессии) → Пароль → UI
```

### TOTP
```
TOTP секрет → Кузнечик CTR → БД
БД → Кузнечик CTR → TOTP секрет → pyotp → 6-значный код
```

## 📊 Размеры файлов

| Файл | Размер | Описание |
|------|--------|----------|
| app.py | 16 KB | Backend логика |
| models.py | 6 KB | Модели БД |
| crypto_gost.py | 10 KB | Криптография |
| backup.py | 8 KB | Резервное копирование |
| static/app.js | 19 KB | Frontend логика |
| static/index.html | 10 KB | HTML структура |
| static/style.css | 10 KB | Стили |
| README.md | 12 KB | Документация |
| FAQ.md | 14 KB | Вопросы-ответы |
| SECURITY.md | 11 KB | Безопасность |
| **Всего** | **~120 KB** | Исходный код |

## 🔐 Безопасность

### Шифрование
- **Алгоритм**: ГОСТ Р 34.12-2015 (Кузнечик)
- **Режим**: CTR (Counter)
- **Размер ключа**: 256 бит
- **Nonce**: 128 бит (уникальный для каждого шифрования)

### Хэширование
- **Мастер-пароль**: Argon2id (memory=64MB, time=3, parallelism=4)
- **Деривация ключей**: PBKDF2-HMAC-SHA512 (100k итераций) + Стрибог-256
- **Соль**: 256 бит криптографически стойкая

### Защита
- Rate limiting (5 попыток → 30 сек блокировка)
- Автоблокировка (5 мин неактивности)
- HttpOnly cookies
- CSRF защита
- Zero-knowledge архитектура

## 🚀 Производительность

### Скорость операций
- **Вход**: ~1-2 сек (Argon2id)
- **Шифрование**: <10 мс на запись
- **Расшифрование**: <10 мс на запись
- **Поиск**: <50 мс (до 1000 записей)

### Требования
- **RAM**: 100 MB (приложение) + 64 MB (Argon2id)
- **CPU**: Любой современный процессор
- **Диск**: 10 MB (приложение) + размер БД

## 📈 Масштабируемость

### Ограничения
- **Записи**: До 10,000 (рекомендуется <5,000)
- **Размер БД**: До 100 MB (практически)
- **Пользователи**: 1 (локальное использование)

### Оптимизация
- Индексы в БД для быстрого поиска
- Кэширование расшифрованных данных в памяти
- Ленивая загрузка записей
- Пагинация (планируется)

---

**Последнее обновление**: 2025-11-30
