# 🎯 Первый запуск - Пошаговая инструкция

Это руководство проведет вас через первый запуск менеджера паролей.

## ⚡ Быстрый путь (для опытных пользователей)

```bash
# Windows
start.bat

# Linux/macOS
chmod +x start.sh
./start.sh
```

Откройте: **https://127.0.0.1:5000**

## 📋 Подробная инструкция (для новичков)

### Шаг 1: Проверка Python

#### Windows
1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. В командной строке введите:
   ```bash
   python --version
   ```
4. Должно показать: `Python 3.9.x` или выше

**Если Python не установлен**:
1. Перейдите на https://www.python.org/downloads/
2. Скачайте последнюю версию Python 3
3. Запустите установщик
4. ✅ **ВАЖНО**: Отметьте "Add Python to PATH"
5. Нажмите "Install Now"
6. Дождитесь завершения установки
7. Перезапустите командную строку

#### Linux
```bash
# Проверка версии
python3 --version

# Если не установлен (Ubuntu/Debian)
sudo apt update
sudo apt install python3 python3-pip python3-venv

# Если не установлен (Fedora)
sudo dnf install python3 python3-pip

# Если не установлен (Arch)
sudo pacman -S python python-pip
```

#### macOS
```bash
# Проверка версии
python3 --version

# Если не установлен, используйте Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python3
```

### Шаг 2: Открытие проекта

#### Windows
1. Откройте Проводник
2. Перейдите в папку проекта: `C:\Users\Michael\Documents\ReNew\5`
3. В адресной строке введите `cmd` и нажмите Enter
4. Откроется командная строка в папке проекта

#### Linux/macOS
```bash
cd /path/to/password-manager-gost
```

### Шаг 3: Запуск приложения

#### Автоматический запуск (рекомендуется)

**Windows**:
1. Двойной клик на файл `start.bat`
2. Откроется окно командной строки
3. Дождитесь сообщения "Running on https://127.0.0.1:5000"

**Linux/macOS**:
```bash
chmod +x start.sh
./start.sh
```

#### Ручной запуск (если автоматический не работает)

```bash
# 1. Создание виртуального окружения
python -m venv venv

# 2. Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Создание .env файла
copy .env.example .env  # Windows
cp .env.example .env    # Linux/macOS

# 5. Запуск приложения
python app.py
```

### Шаг 4: Открытие в браузере

1. Откройте браузер (Chrome, Firefox, Edge)
2. Введите адрес: **https://127.0.0.1:5000**
3. Нажмите Enter

#### ⚠️ Предупреждение о сертификате

Браузер покажет предупреждение о безопасности. **Это нормально!**

**Chrome**:
1. Нажмите "Дополнительно"
2. Нажмите "Перейти на сайт 127.0.0.1 (небезопасный)"

**Firefox**:
1. Нажмите "Дополнительно"
2. Нажмите "Принять риск и продолжить"

**Edge**:
1. Нажмите "Подробности"
2. Нажмите "Перейти на веб-страницу"

**Safari**:
1. Нажмите "Показать подробности"
2. Нажмите "Посетить этот веб-сайт"

### Шаг 5: Создание мастер-пароля

1. Вы увидите экран "Создание мастер-пароля"
2. Придумайте **надежный пароль**:
   - Минимум 15 символов
   - Заглавные и строчные буквы
   - Цифры
   - Специальные символы

**Примеры хороших паролей**:
- `MySecure#Password2025!`
- `Tr0pic@l-Sunr!se-2025`
- `Quantum$Leap#2025!Safe`

3. Введите пароль в первое поле
4. Повторите пароль во втором поле
5. Проверьте индикатор стойкости пароля
6. Нажмите "Создать мастер-пароль"

#### 🔴 КРИТИЧЕСКИ ВАЖНО!

**Запишите мастер-пароль в безопасном месте!**

- ✅ Запишите на бумаге и храните в сейфе
- ✅ Используйте менеджер паролей (если есть другой)
- ✅ Сфотографируйте и храните в зашифрованном виде
- ❌ НЕ храните в текстовом файле на компьютере
- ❌ НЕ отправляйте по email или мессенджерам

**Если вы забудете мастер-пароль, восстановление НЕВОЗМОЖНО!**

### Шаг 6: Вход в систему

1. После создания мастер-пароля появится экран входа
2. Введите ваш мастер-пароль
3. Нажмите "Войти"
4. Вы попадете на главный экран приложения

### Шаг 7: Добавление первого пароля

1. Нажмите кнопку "+ Добавить запись"
2. Заполните форму:

   **Название сайта** (обязательно):
   ```
   Google
   ```

   **URL** (опционально):
   ```
   https://accounts.google.com
   ```

   **Логин/Email** (обязательно):
   ```
   your.email@gmail.com
   ```

   **Пароль** (обязательно):
   - Нажмите "🎲 Генерировать"
   - Настройте длину (рекомендуется 20+)
   - Нажмите "Генерировать"
   - Нажмите "Использовать"

   **Заметки** (опционально):
   ```
   Основной аккаунт Google
   ```

3. Нажмите "Сохранить"

🎉 **Поздравляем!** Ваш первый пароль сохранен!

### Шаг 8: Тестирование функций

#### Копирование пароля
1. Найдите вашу запись
2. Нажмите 📋 рядом с паролем
3. Появится уведомление "Скопировано в буфер обмена"
4. Вставьте пароль где угодно (Ctrl+V)

#### Показ/скрытие пароля
1. Нажмите 👁️ рядом с паролем
2. Пароль станет видимым
3. Нажмите еще раз, чтобы скрыть

#### Поиск
1. Введите "Google" в поле поиска вверху
2. Список отфильтруется автоматически

#### Редактирование
1. Нажмите "✏️ Изменить" на карточке записи
2. Измените любые поля
3. Нажмите "Сохранить"

### Шаг 9: Создание резервной копии

**Это очень важно!**

#### Через командную строку
```bash
python backup.py create
```

#### Вручную
1. Найдите файл `password_manager.db` в папке проекта
2. Скопируйте его в безопасное место
3. Назовите копию, например: `backup_2025-11-30.db`

**Рекомендации**:
- Создавайте копии еженедельно
- Храните на внешнем носителе (USB, внешний HDD)
- Используйте облачное хранилище с шифрованием

### Шаг 10: Настройка автозапуска (опционально)

#### Windows
1. Нажмите `Win + R`
2. Введите `shell:startup` и нажмите Enter
3. Создайте ярлык на `start.bat` в этой папке

#### Linux (systemd)
Создайте файл `~/.config/systemd/user/password-manager.service`:
```ini
[Unit]
Description=Password Manager GOST

[Service]
Type=simple
WorkingDirectory=/path/to/password-manager-gost
ExecStart=/path/to/password-manager-gost/venv/bin/python app.py

[Install]
WantedBy=default.target
```

Затем:
```bash
systemctl --user enable password-manager
systemctl --user start password-manager
```

## 🎓 Что дальше?

### Изучите документацию
- [README.md](README.md) - Полное руководство
- [FAQ.md](FAQ.md) - Ответы на вопросы
- [SECURITY.md](SECURITY.md) - Безопасность

### Добавьте больше паролей
- Импортируйте пароли из браузера (вручную)
- Добавьте все важные аккаунты
- Настройте TOTP для 2FA

### Настройте резервное копирование
- Создайте расписание резервного копирования
- Настройте автоматическую синхронизацию с облаком
- Проверьте восстановление из копии

## ❓ Возникли проблемы?

### Python не найден
```
'python' is not recognized as an internal or external command
```

**Решение**: Установите Python и добавьте в PATH (см. Шаг 1)

### Порт 5000 занят
```
Address already in use
```

**Решение**: Измените порт в `app.py`:
```python
app.run(host='127.0.0.1', port=5001, ...)
```

### Ошибка установки зависимостей
```
ERROR: Could not install packages
```

**Решение**:
```bash
pip install --upgrade pip
pip install --no-cache-dir -r requirements.txt
```

### Браузер не открывается
**Решение**: Откройте вручную: https://127.0.0.1:5000

### Приложение не запускается
**Решение**: Проверьте логи в консоли и создайте issue с описанием ошибки

## 📞 Поддержка

- 📖 Читайте [FAQ.md](FAQ.md)
- 🔒 Вопросы безопасности → [SECURITY.md](SECURITY.md)
- 🐛 Нашли ошибку? Создайте issue
- 💬 Общие вопросы → создайте discussion

## ✅ Контрольный список первого запуска

- [ ] Python 3.9+ установлен
- [ ] Проект скачан/клонирован
- [ ] Зависимости установлены
- [ ] Приложение запущено
- [ ] Браузер открыт на https://127.0.0.1:5000
- [ ] Мастер-пароль создан и записан
- [ ] Первая запись добавлена
- [ ] Резервная копия создана
- [ ] Документация изучена

---

**Готово! Теперь вы можете безопасно хранить все свои пароли! 🔐**

**Следующий шаг**: Прочитайте [SECURITY.md](SECURITY.md) для максимальной безопасности.
