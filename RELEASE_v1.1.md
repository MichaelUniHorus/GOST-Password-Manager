# 🎉 Release Notes v1.1 — Улучшения безопасности и UX

## 📅 Дата релиза: 2025-11-30

## 🎯 Основные изменения

### ✨ Новые функции

#### 0. **Веб-интерфейс для новых функций** ⭐
- **Кнопка "⚙️ Настройки"** в header'е
- **Модальное окно смены мастер-пароля**
  - Валидация в реальном времени
  - Индикатор стойкости пароля (слабый/средний/сильный)
  - Проверка совпадения паролей
- **Модальное окно настроек автобэкапов**
  - Включение/выключение
  - Выбор частоты (daily/weekly/monthly)
  - Количество копий для хранения
  - Кнопка ручного создания копии
  - Отображение времени последнего бэкапа
- **Уведомления**
  - Всплывающие уведомления в правом верхнем углу
  - Автоматическое исчезновение через 3 секунды
  - Анимация появления/исчезновения

#### 1. Смена мастер-пароля без пересоздания БД
- **Архитектура MEK (Master Encryption Key)**
  - Введен отдельный ключ шифрования (MEK), который шифрует все данные
  - MEK сам зашифрован ключом, полученным из мастер-пароля
  - При смене пароля перешифровывается только MEK, а не все записи
  
- **API endpoint**: `POST /api/change-master-password`
  - Параметры: `current_password`, `new_password`
  - Проверка стойкости нового пароля
  - Автоматическая миграция для существующих БД

- **Безопасность**
  - Проверка текущего пароля перед сменой
  - Валидация стойкости нового пароля (минимум 15 символов)
  - Логирование в аудит
  - Автоматическое обновление ключа в текущей сессии

#### 2. Автоматическое резервное копирование
- **Новый сервис**: `auto_backup.py`
  - Фоновый процесс для автоматических бэкапов
  - Поддержка расписаний: daily, weekly, monthly
  - Автоматическая очистка старых копий
  
- **Настройки автобэкапов**
  - API endpoints:
    - `GET /api/backup-settings` — получить настройки
    - `PUT /api/backup-settings` — обновить настройки
  - Параметры:
    - `enabled` — включить/выключить
    - `frequency` — частота (daily/weekly/monthly)
    - `keep_count` — количество копий для хранения
    - `backup_path` — путь к директории бэкапов

- **Запуск сервиса**:
  ```bash
  # Запуск в фоне
  python auto_backup.py
  
  # Тестовый бэкап
  python auto_backup.py --test
  ```

#### 3. Unit-тесты криптомодуля
- **Новый файл**: `test_crypto.py`
- **Покрытие**:
  - Генерация соли и nonce
  - Хэширование Стрибог-512/256
  - Деривация ключей PBKDF2-ГОСТ
  - Хэширование и проверка мастер-пароля (Argon2id)
  - Шифрование/расшифрование данных (Кузнечик CTR)
  - Генерация и валидация паролей
  - Работа с MEK (генерация, шифрование, расшифрование)

- **Запуск тестов**:
  ```bash
  pytest test_crypto.py -v
  
  # С покрытием
  pytest test_crypto.py --cov=crypto_gost --cov-report=html
  ```

### 🔧 Технические изменения

#### Модели базы данных (`models.py`)
- **Новая таблица**: `master_encryption_key`
  - `encrypted_key` — зашифрованный MEK
  - `kdf_salt` — соль для KDF
  - `kdf_iterations` — количество итераций PBKDF2
  - `created_at`, `updated_at` — временные метки

- **Новая таблица**: `backup_settings`
  - `enabled` — включено ли автобэкапирование
  - `frequency` — частота (daily/weekly/monthly)
  - `keep_count` — количество копий
  - `last_backup` — время последнего бэкапа
  - `backup_path` — путь к директории

#### Криптография (`crypto_gost.py`)
- **Новые методы**:
  - `generate_mek()` — генерация MEK
  - `encrypt_mek(mek, password, salt)` — шифрование MEK
  - `decrypt_mek(encrypted_mek, password, salt)` — расшифрование MEK

#### Зависимости (`requirements.txt`)
- **Добавлено**:
  - `pytest==7.4.3` — фреймворк тестирования
  - `pytest-cov==4.1.0` — покрытие кода тестами
  - `schedule==1.2.0` — планировщик задач для автобэкапов

### 📊 Статистика

- **Новых файлов**: 3
  - `test_crypto.py` — 200+ строк тестов
  - `auto_backup.py` — 150+ строк
  - `RELEASE_v1.1.md` — этот файл

- **Изменено файлов**: 3
  - `models.py` — +2 таблицы
  - `crypto_gost.py` — +3 метода для MEK
  - `app.py` — +3 API endpoints

- **Новых API endpoints**: 3
  - `POST /api/change-master-password`
  - `GET /api/backup-settings`
  - `PUT /api/backup-settings`

## 🚀 Установка и обновление

### Для новых пользователей
```bash
pip install -r requirements.txt
python app.py
```

### Для существующих пользователей

1. **Обновить зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

2. **Миграция БД** (автоматическая):
   - При первом запуске новые таблицы создадутся автоматически
   - MEK будет создан при первой смене мастер-пароля

3. **Запустить приложение**:
   ```bash
   python app.py
   ```

4. **(Опционально) Запустить сервис автобэкапов**:
   ```bash
   python auto_backup.py
   ```

## 📖 Использование новых функций

### Смена мастер-пароля

**Через API**:
```bash
curl -X POST https://127.0.0.1:5000/api/change-master-password \
  -H "Content-Type: application/json" \
  -d '{
    "current_password": "OldPassword123!",
    "new_password": "NewSecurePassword456!"
  }'
```

**Через UI** (будет добавлено в следующем релизе):
- Настройки → Безопасность → Сменить мастер-пароль

### Настройка автобэкапов

**Включить ежедневные бэкапы**:
```bash
curl -X PUT https://127.0.0.1:5000/api/backup-settings \
  -H "Content-Type: application/json" \
  -d '{
    "enabled": true,
    "frequency": "daily",
    "keep_count": 10
  }'
```

**Запустить сервис**:
```bash
# В отдельном терминале
python auto_backup.py
```

### Запуск тестов

```bash
# Все тесты
pytest test_crypto.py -v

# Конкретный тест
pytest test_crypto.py::TestGOSTCrypto::test_encrypt_decrypt_mek -v

# С покрытием
pytest test_crypto.py --cov=crypto_gost --cov-report=term-missing
```

## ⚠️ Breaking Changes

**Нет breaking changes** — релиз полностью обратно совместим с v1.0.0.

- Существующие БД работают без изменений
- MEK создается автоматически при первой смене пароля
- Все старые API endpoints работают как прежде

## 🐛 Исправленные ошибки

- Исправлена проблема с `pygost` → заменен на `gostcrypto`
- Улучшена обработка ошибок при шифровании/расшифровании

## 🔮 Что дальше (v1.2)

- Импорт/экспорт в формате KeePass (.kdbx)
- Импорт из CSV
- UI для смены мастер-пароля
- UI для настройки автобэкапов
- Проверка паролей на утечки (haveibeenpwned API)
- Улучшенная оценка стойкости паролей (zxcvbn)

## 📝 Благодарности

Спасибо всем, кто тестировал и давал обратную связь!

---

**Полный changelog**: [CHANGELOG.md](CHANGELOG.md)  
**Roadmap**: [ROADMAP.md](ROADMAP.md)  
**Документация**: [README.md](README.md)
