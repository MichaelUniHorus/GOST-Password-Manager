# 🔐 Менеджер паролей с ГОСТ-криптографией

Локальный менеджер паролей с веб-интерфейсом, соответствующий требованиям РФ 2025 года.

## 🎯 Особенности

### Криптографическая защита по ГОСТ
- **ГОСТ Р 34.12-2015 (Кузнечик)** - шифрование паролей, заметок и TOTP-ключей в режиме CTR
- **ГОСТ Р 34.11-2012 (Стрибог-512)** - хэширование мастер-пароля с PBKDF2 (100 000 итераций)
- **Argon2id** - memory-hard функция для защиты от brute-force атак
- **Zero-knowledge архитектура** - сервер не знает мастер-пароль

### Функциональность
- ✅ Безопасное хранение паролей с шифрованием на клиенте
- ✅ Генератор криптографически стойких паролей (20+ символов)
- ✅ Поддержка TOTP (2FA) с автообновлением кодов
- ✅ Поиск по записям в реальном времени
- ✅ Журнал аудита всех действий
- ✅ Защита от brute-force (5 попыток → блокировка на 30 сек)
- ✅ Автоблокировка при неактивности (5 минут)
- ✅ Современный адаптивный UI

### Безопасность
- 🔒 Все данные шифруются локально перед сохранением
- 🔒 Мастер-пароль минимум 15 символов с проверкой стойкости
- 🔒 Ключи шифрования генерируются детерминированно из мастер-пароля
- 🔒 SQLite база данных с шифрованием
- 🔒 HTTPS с self-signed сертификатом
- 🔒 Защита сессий с HttpOnly cookies

## 📋 Требования

- Python 3.9+
- Windows/Linux/macOS

## 🚀 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установка зависимостей
pip install flask sqlalchemy gostcrypto pyotp argon2-cffi zxcvbn
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и установите свои значения:

```env
SECRET_KEY=your-secret-key-here-change-this
DATABASE_PATH=password_manager.db
SESSION_TIMEOUT=300
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=30
```

### 3. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: **https://127.0.0.1:5000**

⚠️ **Важно:** При первом запуске браузер предупредит о небезопасном соединении из-за self-signed сертификата. Это нормально для локального использования. Нажмите "Дополнительно" → "Продолжить" (Chrome) или аналогичную опцию в вашем браузере.

## 📖 Использование

### Первый запуск

1. Откройте https://127.0.0.1:5000 в браузере
2. Создайте мастер-пароль (минимум 15 символов)
3. Запомните его - восстановление невозможно!

### Добавление записи

1. Нажмите "+ Добавить запись"
2. Заполните поля:
   - **Название сайта** (обязательно)
   - **URL** (опционально)
   - **Логин/Email** (обязательно)
   - **Пароль** (обязательно) - можно сгенерировать автоматически
   - **Заметки** (опционально)
   - **TOTP секрет** (опционально, для 2FA)
3. Нажмите "Сохранить"

### Генерация пароля

1. При создании/редактировании записи нажмите "🎲 Генерировать"
2. Настройте параметры:
   - Длина (15-64 символа)
   - Типы символов (заглавные, строчные, цифры, спецсимволы)
3. Нажмите "Использовать"

### Использование TOTP (2FA)

1. Получите TOTP секрет от сервиса (обычно QR-код)
2. Добавьте секрет в поле "TOTP секрет" при создании записи
3. В списке записей нажмите "Показать TOTP" для получения кода
4. Код автоматически обновляется каждые 30 секунд

### Поиск

Используйте поле поиска для фильтрации записей по:
- Названию сайта
- Логину
- URL

### Журнал аудита

Нажмите "📊 Аудит" для просмотра истории действий:
- Вход/выход
- Создание/изменение/удаление записей
- IP-адреса и временные метки

## 🔧 Архитектура

```
password-manager/
├── app.py                 # Flask приложение (backend)
├── models.py              # Модели базы данных (SQLAlchemy)
├── crypto_gost.py         # Криптографический модуль (ГОСТ)
├── requirements.txt       # Зависимости Python
├── .env                   # Конфигурация (не в git)
├── .env.example           # Пример конфигурации
├── .gitignore            # Исключения для git
├── README.md             # Документация
└── static/               # Frontend
    ├── index.html        # HTML интерфейс
    ├── style.css         # Стили
    └── app.js            # JavaScript логика
```

## 🔐 Криптографические детали

### Шифрование данных

1. **Деривация ключа**: Мастер-пароль → PBKDF2-HMAC-SHA512 (100k итераций) → Стрибог-256
2. **Шифрование**: Кузнечик (ГОСТ Р 34.12-2015) в режиме CTR
3. **Формат**: `base64(nonce[16] + ciphertext)`

### Хранение мастер-пароля

1. **Хэширование**: Argon2id (memory-cost=64MB, time-cost=3, parallelism=4)
2. **Соль**: 32 байта (256 бит) криптографически стойкая
3. **Хранение**: Только хэш, пароль никогда не сохраняется

### Генерация паролей

- **CSPRNG**: Python `secrets` модуль
- **Минимальная длина**: 15 символов (рекомендуется 20+)
- **Набор символов**: Настраиваемый (A-Z, a-z, 0-9, спецсимволы)

## ⚠️ Важные замечания

### Безопасность

1. **Мастер-пароль** - единственный способ доступа к данным. Если вы его забудете, восстановление невозможно!
2. **Резервные копии** - регулярно создавайте копии файла `password_manager.db`
3. **Локальное использование** - приложение предназначено для локального использования, не размещайте его в интернете без дополнительной защиты
4. **HTTPS** - используется self-signed сертификат, для продакшена нужен настоящий сертификат

### Ограничения

- Приложение работает только локально (127.0.0.1)
- Нет синхронизации между устройствами
- Нет облачного бэкапа (только локальные копии)
- Self-signed SSL сертификат (предупреждения браузера)

## 🛠️ Разработка

### Установка в режиме разработки

```bash
# Установка с dev-зависимостями
pip install -r requirements.txt

# Запуск с debug режимом
python app.py
```

### Тестирование

```bash
# Запуск тестов (если добавлены)
pytest tests/
```

### Структура API

#### Инициализация
- `GET /api/init` - Проверка инициализации
- `POST /api/init` - Создание мастер-пароля

#### Аутентификация
- `POST /api/login` - Вход
- `POST /api/logout` - Выход

#### Записи
- `GET /api/entries` - Список записей
- `POST /api/entries` - Создание записи
- `PUT /api/entries/<id>` - Обновление записи
- `DELETE /api/entries/<id>` - Удаление записи

#### Утилиты
- `POST /api/generate-password` - Генерация пароля
- `GET /api/totp/<id>` - Получение TOTP кода
- `GET /api/audit-logs` - Журнал аудита

## 📝 Соответствие требованиям РФ 2025

### Приказ ФСБ РФ №117 от 18.03.2025

✅ **Алгоритмы шифрования**: ГОСТ Р 34.12-2015 (Кузнечик) в режиме CTR  
✅ **Хэширование**: ГОСТ Р 34.11-2012 (Стрибог-512) + PBKDF2 (100k итераций)  
✅ **Генерация ключей**: Детерминированная из мастер-пароля через PBKDF2-ГОСТ  
✅ **Длина паролей**: Минимум 15 символов с проверкой стойкости  
✅ **Защита базы**: SQLite с шифрованием  
✅ **Zero-knowledge**: Сервер не знает мастер-пароль  
✅ **Защита от brute-force**: Argon2id + rate limiting  
✅ **Аудит**: Полное логирование действий (без секретов)  
✅ **Автоблокировка**: 5 минут неактивности  

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
2. Убедитесь, что порт 5000 свободен
3. Проверьте файл `.env` на корректность
4. Посмотрите логи в консоли

## 📄 Лицензия

MIT License - свободное использование с указанием авторства.

## ⚡ Быстрый старт

```bash
# 1. Установка
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt

# 2. Настройка
copy .env.example .env

# 3. Запуск
python app.py

# 4. Открыть браузер
# https://127.0.0.1:5000
```

---

**Важно**: Это приложение предназначено для локального использования. Для продакшена требуется дополнительная настройка безопасности, настоящий SSL-сертификат и возможно использование production WSGI сервера (gunicorn, waitress).
